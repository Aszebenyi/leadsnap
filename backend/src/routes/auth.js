import { Router } from 'express';

const router = Router();

// Auth is handled client-side via Supabase Auth SDK.
// This router is a placeholder for any future server-side auth endpoints
// (e.g. token refresh helpers, session validation checks).

export default router;
